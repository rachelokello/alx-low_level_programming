#include <stdio.h>
int main()
{
	int n;

	srand(time(0));
	n=rand()-RAND_MAX / 2;
	scanf("%d", &n:");
	if(n > 0){
	printf("the number is positive: ");
	}
	else if(n == 0){
		printf("the number is zero:");
	}
	else(n < 0){
		printf("the number is a negative:");
		}
return (0)
}
