#include "main.h"

/**
 * main - check the code
 *
 * Return: always 0
 */
int main(void)
{
	char *str;

	str = "i do not fear computers. i fear the lack of the them - issac asimov";
	_puts(str);
	return (0);
}
